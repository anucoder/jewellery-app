

function Pagination() {
  return (
    <>
      {/* <HomePage/> */}
      <div className="pagination">
                <ul className="pages">
                      <li
                        // key={index}
                        className="hand"
                        // onClick={() => pagination(page + 1)}
                      >
                        1
                      </li>
                    
                </ul>
              </div>
    </>
  );
}

export default Pagination;